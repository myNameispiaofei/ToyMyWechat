name: mywechat
description: A new Flutter application.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
     - images/add_friend_icon.png
     - images/b_group_chat_icon.png
     - images/badge.png
     - images/bubble.png
     - images/camera_icon.png
     - images/collect_money.png
     - images/friend_c_icon.png
     - images/game_icon.png
     - images/game_icon_2.png
     - images/group_chat.png
     - images/Hank.png
     - images/icon_friends_add.png
     - images/icon_right.png
     - images/look_icon.png
     - images/magnifier_icon_b.png
     - images/nearby_icon.png
     - images/new_friend_icon.png
     - images/official_accounts_icon.png
     - images/pentagram_b.png
     - images/pentagram_w.png
     - images/round_plus.png
     - images/search_star.png
     - images/shake.png
     - images/shopping_icon.png
     - images/star_icon.png
     - images/sweep_black.png
     - images/sweep_blue.png
     - images/sweep_w.png
     - images/tabbar_chat.png
     - images/tabbar_chat_hl.png
     - images/tabbar_friends.png
     - images/tabbar_friends_hl.png
     - images/tabbar_mine.png
     - images/tabbar_mine_hl@2x.png
     - images/tip_label_icon.png
     - images/we_applets.png
     - images/we_card_icon.png
     - images/we_collect.png
     - images/we_emoji.png
     - images/we_pay.png
     - images/we_photo_album.png
     - images/we_setting_icon.png
     - images/my_portrait.png
     - images/搜一搜.png
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
